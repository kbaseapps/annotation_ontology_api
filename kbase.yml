module-name:
    annotation_ontology_api

module-description:
    API for accessing and writing annotation ontology terms to genome and annotated metagenome objects

service-language:
    python

module-version:
    0.0.1

owners:
    [chenry]

service-config:
  dynamic-service: true

